<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizzo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/quizwiz_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
         <activity android:name=".MainActivity" />
        <activity android:name=".MakeQuiz" />
        <activity android:name=".RecordQuestions" />
        <activity android:name=".PlayQuiz" />
        <activity android:name=".AnswerQuiz" />
        <activity android:name=".ShowResult"/>
        <activity android:name=".QuizzoPreview">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>